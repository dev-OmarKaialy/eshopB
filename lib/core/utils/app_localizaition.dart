import 'package:flutter/material.dart';

class AppLocalizaition {
  static const arLocal = Locale("ar");
  static const enLocale = Locale("en");
  static List<Locale> get supportedLocales => [arLocal, enLocale];
}
